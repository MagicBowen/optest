backend: npu
chip: ascend
cases:
  - op: custom_square
    dtypes: [float32]
    shapes:
      input0: [2, 2]
      output0: [2, 2]
    attributes:
      backend_config:
        ascend:
          workdir: ./operator
          command: ["python3", "wrong_impl.py", "--dtype", "float32"]
